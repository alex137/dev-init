# Environment files (may contain SSH keys)
.devcontainer/.env

# Editor-generated files
.zed/tasks.json

# Claude Code local settings (user/session-specific)
.claude/

# Temporary files
Makefile.new

# Project registry
projects.reg

# Persistent user dotfiles (created at runtime)
user/
